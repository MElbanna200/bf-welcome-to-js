'use strict';

/* Booleans

TODO: create a few variables(boolean):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/

// declare a variable isHot and initialize it to the value true
const isHot = true;

// declare a variable isStupid and initialize it to the value false
let isStupid = false;

//reassign the value of isStupid to true

isStupid = true;

//read the variables
console.log(isHot);
console.log(isStupid);